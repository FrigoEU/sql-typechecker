{
  "main": "index.tsx",
  "dependencies": {
    "pgsql-ast-parser": "^8.1.1",
    "typescript": "4.1.2"
  },
  "scripts": {
    "check": "tsc -p . --noEmit",
    "build": "node esbuild.js",
    "start": "npm run build && node --enable-source-maps ./out/main.js ",
    "run-debug-server": "npm run build && node --inspect --enable-source-maps ./out/main.js "
  },
  "devDependencies": {
    "@types/node": "^14.14.10",
    "esbuild": "0.8.34",
    "prettier": "2.2.1"
  }
}
